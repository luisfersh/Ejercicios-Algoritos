let numero = 64;

function calcularRaizCuadrada(numero){
    let resultado = Math.sqrt(numero);
    console.log(resultado); // Necesario para imprimir en consola con Node
    return resultado;
}

calcularRaizCuadrada(numero);